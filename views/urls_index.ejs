<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
    crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<header>
  <% include ./partials/_headers %>
</header>

<body style="font-size:20px; padding:50px;">
  <!-- Shorten new link link-->
  <a href='/urls/new'>Create a new TinyURL</a><br><br>
  <!-- Add EJS code here -->
  <% for(let elem in urls) { %>
  <form class="form-inline" action="/urls/<%= elem %>" method="POST">
    <li>
      <a href="/urls/<%= elem %>">
        <%= elem %></a>&nbsp:&nbsp
      <a href=<%=urls[elem] %>>
        <%= urls[elem] %></a>&nbsp-&nbsp
      <button type="submit" class="btn btn-primary mb2" value="<%= elem %>">Edit</button>
      <form class="form-inline" action="/urls/<%= elem %>/delete" method="POST">
        <button type="submit" class="btn btn-primary mb2" value="<%= elem %>">Delete 🗑</button>
      </form>
  </form>
  </li>
  <% } %>
</body>

</html>